requests>=2.28.0
vulnerable-root-package>=1.0.0
missing-root-lib>=2.1.0

# Editable installs pointing to workspace packages
-e ./packages/core
-e ./packages/utils
-e ./tools/scripts
-e ./tools/automation

# Editable installs pointing to missing directories (should trigger detection)
-e ./packages/missing-package
-e ./tools/nonexistent-tool
